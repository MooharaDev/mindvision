# Provision internally before running (staged ahead of time; NO runtime pip):
pymupdf==1.28.0
# only needed for the web console (webapp.py); the CLI pipeline is pymupdf-only:
flask==3.1.3
waitress==3.0.2
